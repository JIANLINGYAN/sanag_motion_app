# Sanag 运动耳机蓝牙协议控制指令

---

## 一、设备信息同步（0xBA）

### 1.1 个人信息设置

App向设备发送个人信息，数据同步

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBA | 0x-- | 见下方 | - |

#### 1. 设置个人信息

**data[3]** ==> 类型标识  
`0x00`：个人信息同步

**Data[4]** ==> 身高(单位/cm)  
如180cm，data[4]=0xB4

**Data[5]-data[7]** ==> 体重(单位/g)  
如60kg，60*1000，data[5]-data[7]=0x00EA60

**Data[8]** ==> 年龄  
如18岁，data[8]=0x12

**Data[9]** ==> 性别  
性别表示，`0x01`男 `0x02`女 `0x00`保密

#### 2. 获取个人信息

向设备获取当前个人信息  
数据域：`0x00FF`: 获取设备个人信息 (1 byte)

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBA | 0x-- | 见下方 | - |

**1. 正常回复数据**

**data[3]** ==> 类型标识  
`0x00`：个人信息同步

**Data[4]** ==> 身高(单位/cm)  
如180cm，data[4]=0xB4

**Data[5]-data[7]** ==> 体重(单位/g)  
如60kg，60*1000，data[5]-data[7]=0x00EA60

**Data[8]** ==> 年龄  
如18岁，data[8]=0x12

**Data[9]** ==> 性别  
性别表示，`0x01`男 `0x02`女 `0x00`保密

**2. 暂无数据的情况**  
特别的，APP获取个人信息，但设备暂无已设置数据时，设备返回数据域：`0x00FF` (1 byte)

---

### 1.2 时间同步设置

App向设备发起时间同步

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBA | 0x-- | 见下方 | - |

**1. 时间同步**

**data[3]** ==> 类型标识  
`0x01`：时间同步

**Data[4]** ==> 时区  
东时区 1至12，西时区 -1至-12  
如东八区 data[4]=0x08，西-12对应0xF4

**Data[5] - data[17]** ==> 时间戳  
currentTimeMillis ==>（时间戳）：1763358724737  
Data[5]-data[17]==>0x01070603030508070204070307 (1 byte)

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBA | 0x02 | 见下方 | - |

**data[3]** ==> 类型标识  
`0x01`：时间同步

**Data[4]**:
- `0x00` ==> 设置成功
- `0x01` ==> 设置失败

---

### 1.3 心率区间设置

App向设备发送心率区间设置

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBA | 0x-- | 见下方 | - |

**1. 心率区间设置**

**data[3]** ==> 类型标识  
`0x02`：心率区间设置

**Data[4]** ==> 设置区间个数  
这里假定五个，data[4] = 0x05

**Data[5] - data[n]** ==> 心率区间数据

| 区间 | 数据位置 | 示例 |
|------|----------|------|
| 区间一，热身区 | Data[5]-data[6] | 如热身区 75bpm - 85bpm，data[5]-data[6] = 0x4B 0x55 |
| 区间二，有氧耐力区 | Data[7]-data[8] | - |
| 区间三 | Data[9]-data[10] | - |
| 区间四 | Data[11]-data[12] | - |
| 区间五 | Data[13]-data[14] | - |
| ... | ... | ... |

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBA | 0x02 | 见下方 | - |

**data[3]** ==> 类型标识  
`0x02`：心率区间设置

**Data[4]**:
- `0x00` ==> 设置成功
- `0x01` ==> 设置失败

---

### 1.4 乳酸心率乳酸配速区间设置

App向设备发送乳酸心率及乳酸配速区间设置

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBA | 0x-- | 见下方 | - |

**1. 乳酸心率及乳酸配速设置**

**data[3]** ==> 类型标识  
`0x03`：乳酸心率及乳酸配速区间设置

**Data[4]** ==> 是否启用乳酸区间  
- `0x00` ==> 启用（默认启用）
- `0x01` ==> 关闭

**Data[5]-data[6]** ==> 乳酸区间心率  
150bpm - 180bpm → 0x96 0xB4

**Data[7]-data[8]** ==> 乳酸区间最低配速（单位：秒/公里）  
5分钟/km → 5*60=300 → 0x012C

**Data[9]-data[10]** ==> 乳酸区间最高配速（单位：秒/公里）

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBA | 0x02 | 见下方 | - |

**data[3]** ==> 类型标识  
`0x03`：乳酸心率及乳酸配速区间设置

**Data[4]**:
- `0x00` ==> 设置成功
- `0x01` ==> 设置失败

---

### 1.5 设备的绑定和解绑（待定，需与固件确认）

App向设备发送

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBA | 0x-- | 见下方 | - |

**1. 设备绑定和解绑**

**data[3]** ==> 类型标识  
`0x04`：绑定和解绑

**Data[4]** ==> 绑定解绑状态  
- `0x01` ==> 绑定
- `0x02` ==> 解绑

**Data[5]-data[8]** ==> 用户id  
如12345，data[5]-data[8]=0x00003039

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBA | 0x03 | 见下方 | - |

**data[3]** ==> 类型标识  
`0x04`：绑定和解绑

**Data[4]**:
- `0x01` ==> 绑定
- `0x02` ==> 解绑

**Data[5]**:
- `0x00` ==> 设置成功
- `0x01` ==> 设置失败
- `0x02` ==> 已被绑定

---

## 二、运动目标（0xBB）

### 2.1 运动距离目标设置与数据读取

App向设备发送运动距离目标数据或获取当前设置

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBB | 0x-- | 见下方 | - |

**1. 运动距离目标设置**

**data[3]** ==> 类型标识  
`0x00`：运动目标设置

**Data[4]-data[6]** ==> 目标距离（单位:米）  
10公里 → 10*1000=10000，Data[4]-data[6]=0x002710

**2. 获取运动距离目标**  
数据域数据：类型标识+FF ==> `0x00FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBB | 0x04 | 见下方 | - |

**1. 运动距离目标设置**

**data[3]** ==> 类型标识  
`0x00`：运动目标设置

**Data[4]-data[6]** ==> 目标距离（单位:米）  
10公里 → 10*1000=10000，Data[4]-data[6]=0x002710

**2. 获取当前运动距离目标**  
如果未设置过目标数据返回默认值5公里（5000）即，Data[4]-data[6]=0x001388

---

### 2.2 运动消耗目标设置与数据读取

App向设备发送运动消耗目标数据或获取当前设置

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBB | 0x-- | 见下方 | - |

**1. 运动消耗目标设置**

**data[3]** ==> 类型标识  
`0x01`：消耗目标设置

**Data[4]-data[6]** ==> 目标消耗（单位:卡）  
范围：0-6000000卡（0-6000大卡）  
3000大卡 → 3000*1000=3000000，Data[4]-data[6]=0x2DC6C0

**2. 获取运动消耗目标**  
数据域数据：类型标识+FF ==> `0x01FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBB | 0x04 | 见下方 | - |

**1. 运动消耗目标设置**

**data[3]** ==> 类型标识  
`0x01`：消耗目标设置

**Data[4]-data[6]** ==> 目标消耗（单位：卡）  
3000大卡 → 3000*1000=3000000，Data[4]-data[6]=0x2DC6C0

**2. 获取当前运动消耗目标**  
如果未设置过目标数据返回默认值2000大卡（2000000）即，Data[4]-data[6]=0x1E8480

---

### 2.3 运动步数目标设置与数据读取

App向设备发送运动步数目标数据或获取当前设置

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBB | 0x-- | 见下方 | - |

**1. 运动步数目标设置**

**data[3]** ==> 类型标识  
`0x02`：步数目标设置

**Data[4]-data[6]** ==> 目标步数（单位：步）  
范围：0-60000步  
30000步 → 30000，Data[4]-data[6]=0x007530

**2. 获取运动步数目标**  
数据域数据：类型标识+FF ==> `0x02FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBB | 0x04 | 见下方 | - |

**1. 运动步数目标设置**

**data[3]** ==> 类型标识  
`0x02`：步数目标设置

**Data[4]-data[6]** ==> 目标步数（单位/步）  
30000步 → 30000，Data[4]-data[6]=0x007530

**2. 获取当前运动步数目标**  
如果未设置过目标数据返回默认值10000步（10000）即，Data[4]-data[6]=0x002710

---

### 2.4 获取当天步数、卡路里、距离

App向设备发送步数、卡路里、距离

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBB | 0x-- | 见下方 | - |

**获取步数、卡路里、距离**

**data[3]** ==> 类型标识  
`0x03`：步数、卡路里、距离

**Data[4]-data[6]** ==> 日期（年-月-日）  
- Data[4] ==> 年，如2025==>25==>0x19
- Data[5] ==> 月，1-12
- Data[6] ==> 日

如2025年10月14日，Data[4]-data[6]=0x190A0E

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBB | 0x-- | 见下方 | - |

**获取步数、卡路里、距离**

**data[3]** ==> 类型标识  
`0x03`：步数、卡路里、距离

**步数：**

**Data[4]-data[6]** ==> 步数（单位：步）  
范围：0-60000步  
30000步 → 30000，Data[4]-data[6]=0x7530

**卡路里：**

**Data[7]-data[9]** ==> 目标消耗（单位：卡）  
3000大卡 → 3000*1000=3000000，Data[7]-data[9]=0x2DC6C0

**距离：**

**Data[10]-data[12]** ==> 目标距离（单位:米）  
10公里 → 10*1000=10000，Data[10]-data[12]=0x002710

---

## 三、健康检测相关（0xBC）

### 3.1 血氧监测设置

App向设备发送佩戴血氧监测设置与获取指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 血氧监测设置**

**data[3]** ==> 类型标识  
`0x00`：血氧监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**Data[5]** ==> 监测时间间隔（单位：分钟）  
默认值10分钟，如10分钟检测一次，data[5] = 0x0A

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x00FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x03 | 见下方 | - |

**1. 血氧监测设置**

**data[3]** ==> 类型标识  
`0x00`：血氧监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**Data[5]** ==> 监测时间间隔（单位：分钟）  
默认值10分钟，如10分钟检测一次，data[5] = 0x0A

---

### 3.2 心率监测设置

App向设备发送心率监测设置与获取指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 心率监测设置**

**data[3]** ==> 类型标识  
`0x01`：心率监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**Data[5]** ==> 监测时间间隔（单位：分钟）  
默认值10分钟，如10分钟检测一次，data[5] = 0x0A

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x01FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x03 | 见下方 | - |

**1. 心率监测设置**

**data[3]** ==> 类型标识  
`0x01`：心率监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**Data[5]** ==> 监测时间间隔（单位：分钟）  
默认值10分钟，如10分钟检测一次，data[5] = 0x0A

---

### 3.3 颈椎健康监测设置

App向设备发送颈椎健康监测设置与获取指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 颈椎健康监测设置**

**data[3]** ==> 类型标识  
`0x02`：颈椎健康监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x02FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x02 | 见下方 | - |

**1. 颈椎健康监测设置**

**data[3]** ==> 类型标识  
`0x02`：颈椎健康监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

---

### 3.4 跌倒监测设置

App向设备发送跌倒监测设置与获取指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 跌倒监测设置**

**data[3]** ==> 类型标识  
`0x03`：跌倒监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x03FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x02 | 见下方 | - |

**1. 跌倒监测设置**

**data[3]** ==> 类型标识  
`0x03`：跌倒监测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

---

### 3.5 血氧数据读取与设备主动上报

App向设备发送血氧数据获取指令，设备返回指定日期的血氧监测数据。  
由于一天的数据有多条，APP发送该指令后，设备按格式逐条返回，直到返回完毕。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**获取血氧数据**

**data[3]** ==> 类型标识  
`0x04`：血氧数据读取

**Data[4]-data[6]** ==> 日期（年-月-日）  
- Data[4] ==> 年，如2025==>25==>0x19
- Data[5] ==> 月，1-12
- Data[6] ==> 日

如2025年10月14日，Data[4]-data[6]=0x190A0E

---

#### 设备返回格式

设备根据日期返回当天的血氧监测数据，每个时间戳对应一个数据值。每条数据包含数据标识（0：第一条数据，1：中间数据，2：结束数据），用于标识数据返回完成。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x-- | 见下方 | - |

**血氧数据上报**

**data[3]** ==> 类型标识  
`0x04`：血氧数据读取

**Data[4]** ==> 时，24小时制

**Data[5]** ==> 分  
如12:00，Data[4]=0x0C，Data[5]=0x00

**Data[6]** ==> 血氧值（单位：%）  
如血氧值98%，data[6]=0x62

**Data[7]** ==> 数据标识  
- `0x00` ==> 第一条数据
- `0x01` ==> 中间数据
- `0x02` ==> 结束数据
- `0xFF` ==> 主动上报的单条数据

**示例**：  
12:00对应98%，数据标识0x00  
12:10对应95%，数据标识0x01  
...（循环多条数据，最后一条标识为0x02）

---

#### 设备主动上报血氧数据

设备检测到血氧数据有更新时，主动上报给App，格式同上述返回格式（`0xFF`单条数据刷新）。

---

### 3.6 心率数据读取与设备主动上报

App向设备发送心率数据获取指令，设备返回指定日期的心率监测数据。  
由于一天的数据有多条，APP发送该指令后，设备按格式逐条返回，直到返回完毕。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**获取心率数据**

**data[3]** ==> 类型标识  
`0x05`：心率数据读取

**Data[4]-data[6]** ==> 日期（年-月-日）  
- Data[4] ==> 年，如2025==>25==>0x19
- Data[5] ==> 月，1-12
- Data[6] ==> 日

如2025年10月14日，Data[4]-data[6]=0x190A0E

---

#### 设备返回格式

设备根据日期返回当天的心率监测数据，每个时间戳对应一个数据值。每条数据包含数据标识（0：第一条数据，1：中间数据，2：结束数据），用于标识数据返回完成。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x-- | 见下方 | - |

**心率数据上报**

**data[3]** ==> 类型标识  
`0x05`：心率数据读取

**Data[4]** ==> 时，24小时制

**Data[5]** ==> 分  
如12:00，Data[4]=0x0C，Data[5]=0x00

**Data[6]** ==> 心率值（bpm）  
如心率值80bpm，data[6]=0x50

**Data[7]** ==> 数据标识  
- `0x00` ==> 第一条数据
- `0x01` ==> 中间数据
- `0x02` ==> 结束数据
- `0xFF` ==> 主动上报的单条数据

**示例**：  
12:00对应80bpm，数据标识0x00  
12:10对应75bpm，数据标识0x01  
...（循环多条数据，最后一条标识为0x02）

---

#### 设备主动上报心率数据

设备检测到心率数据有更新时，主动上报给App，格式同上述返回格式（`0xFF`单条数据刷新）。

---

### 3.7 颈椎健康数据读取与设备主动上报

App向设备发送颈椎健康数据获取指令，设备返回指定日期的颈椎健康监测数据（总佩戴时长和level 1/2/3/4/5的占比）。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**获取颈椎健康数据**

**data[3]** ==> 类型标识  
`0x06`：颈椎健康数据读取

**Data[4]-data[6]** ==> 日期（年-月-日）  
- Data[4] ==> 年，如2025==>25==>0x19
- Data[5] ==> 月，1-12
- Data[6] ==> 日

如2025年10月14日，Data[4]-data[6]=0x190A0E

---

#### 设备返回格式

设备根据日期返回当天的颈椎健康监测数据，包含总佩戴时长和5个档位（level 1/2/3/4/5）的占比。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x-- | 见下方 | - |

**颈椎健康数据上报**

**data[3]** ==> 类型标识  
`0x06`：颈椎健康数据读取

**Data[4]-data[7]** ==> 总佩戴时长（单位：分钟）  
如佩戴8小时，8*60=480分钟，Data[4]-data[7]=0x000001E0

| 数据位 | 说明 |
|--------|------|
| Data[8] | Level 1占比（单位：%），如占比20%，data[8]=0x14 |
| Data[9] | Level 2占比（单位：%） |
| Data[10] | Level 3占比（单位：%） |
| Data[11] | Level 4占比（单位：%） |
| Data[12] | Level 5占比（单位：%） |

---

#### 设备主动上报颈椎健康数据

设备检测到颈椎健康数据有更新时，主动上报给App，格式同上述返回格式。

---

### 3.8 紧急联系人设置

App向设备发送紧急联系人设置与获取指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 紧急联系人设置**

**data[3]** ==> 类型标识  
`0x07`：紧急联系人设置

**Data[4]-data[5]** ==> 国际区号  
如中国 +86，data[4]-data[5]=0x0056

**Data[6]** ==> n 电话号码长度

**Data[7]-data[7 + n-1]** ==> 电话号码

如+86 13230650700  
Data[4] - data[7+n-1] = 0x0056 + 0x0B + 0x0103020300060500070000

**2. 获取当前紧急联系人设置**  
数据域数据：类型标识+FF ==> `0x07FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x-- | 见下方 | - |

**1. 紧急联系人设置**

**data[3]** ==> 类型标识  
`0x07`：紧急联系人设置

**Data[4]-data[5]** ==> 国际区号  
如中国 +86，data[4]-data[5]=0x0056

**Data[6]** ==> n 电话号码长度

**Data[7]-data[7 + n-1]** ==> 电话号码

如+86 13230650700  
Data[4] - data[7+n-1] = 0x0056 + 0x0B + 0x0103020300060500070000

**2. APP主动获取当前联系人时**  
如果从未设置过联系人，设备回复，类型标识+0xFF：`0x07FF`默认值

---

### 3.9 APP下发主动血氧测量指令

App向设备发送血氧测量指令，下发指令包含开始测量、结束测量。  
设备开始测量后，主动上报血氧数据，按3.5中的数据格式上报。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 血氧测量设置**

**data[3]** ==> 类型标识  
`0x08`：血氧测量开始与关闭

**Data[4]** ==> 测量状态  
- `0x00` ==> 关闭（默认）
- `0x01` ==> 开始

**2. 血氧测量当前状态获取**  
获取状态发送 类型标识 + 0xFF，即 `0x08FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x-- | 见下方 | - |

**血氧测量**

**data[3]** ==> 类型标识  
`0x08`：血氧测量开始与关闭

**Data[4]** ==> 测量状态  
- `0x00` ==> 关闭（默认）
- `0x01` ==> 开始

---

### 3.10 APP下发主动心率测量指令

App向设备发送心率测量指令，下发指令包含开始测量、结束测量。  
设备开始测量后，主动上报心率数据，按3.6中的数据格式上报。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 心率测量设置**

**data[3]** ==> 类型标识  
`0x09`：心率测量开始与关闭

**Data[4]** ==> 测量状态  
- `0x00` ==> 关闭（默认）
- `0x01` ==> 开始

**2. 心率测量当前状态获取**  
获取状态发送 类型标识 + 0xFF，即 `0x09FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x-- | 见下方 | - |

**心率测量**

**data[3]** ==> 类型标识  
`0x09`：心率测量开始与关闭

**Data[4]** ==> 测量状态  
- `0x00` ==> 关闭（默认）
- `0x01` ==> 开始

---

### 3.11 久坐提醒设置

App向设备发送久坐提醒设置与获取指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 久坐提醒设置**

**data[3]** ==> 类型标识  
`0x0A`：久坐提醒

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x0AFF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x03 | 见下方 | - |

**1. 久坐提醒设置**

**data[3]** ==> 类型标识  
`0x0A`：久坐提醒

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

---

### 3.12 心率广播设置

App向设备发送心率广播设置与获取指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x02 | 见下方 | - |

**1. 心率广播设置**

**data[3]** ==> 类型标识  
`0x0B`：心率广播设置

**Data[4]** ==> 心率广播开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x0BFF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x02 | 见下方 | - |

**1. 心率广播设置**

**data[3]** ==> 类型标识  
`0x0B`：心率广播设置

**Data[4]** ==> 心率广播开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x0BFF`

---

### 3.13 颈椎传感器佩戴检测

App向设备发送颈椎传感器佩戴检测

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x02 | 见下方 | - |

**1. 颈椎传感器佩戴检测**

**data[3]** ==> 类型标识  
`0x0C`：颈椎传感器佩戴检测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x0CFF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x02 | 见下方 | - |

**1. 颈椎传感器佩戴检测**

**data[3]** ==> 类型标识  
`0x0C`：颈椎传感器佩戴检测

**Data[4]** ==> 监测开关  
- `0x00` ==> 打开(默认值)
- `0x01` ==> 关闭

---

### 3.14 颈椎传感器校准

App向设备发送颈椎传感器校准

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x02 | 见下方 | - |

**1. 传感器校准**

**data[3]** ==> 类型标识  
`0x0D`：传感器校准

**Data[4]** ==> 传感器校准  
- `0x01` ==> 第一步直视
- `0x02` ==> 第二步低头
- `0x03` ==> 第三步回正

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x03 | 见下方 | - |

**1. 传感器校准**

**data[3]** ==> 类型标识  
`0x0D`：传感器校准

**Data[4]** ==> 传感器校准  
- `0x01` ==> 第一步直视
- `0x02` ==> 第二步低头
- `0x03` ==> 第三步回正

**Data[5]** ==> 传感器校准结果  
- `0x00` ==> 成功
- `0x01` ==> 失败

---

### 3.15 颈椎舒展提醒设置

App向设备发送颈椎舒展提醒设置

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 颈椎舒展提醒设置**

**data[3]** ==> 类型标识  
`0x0E`：颈椎舒展提醒设置

**Data[4]** ==> 监测时间间隔（单位：分钟）  
范围值10 -- 60分钟  
如10分钟检测一次，data[4] = 0x0A

**2. 获取当前监测设置**  
数据域数据：类型标识+FF ==> `0x0EFF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x03 | 见下方 | - |

**1. 颈椎舒展提醒设置**

**data[3]** ==> 类型标识  
`0x0E`：颈椎舒展提醒设置

**Data[4]** ==> 监测时间间隔（单位：分钟）  
范围值10 -- 60分钟  
如10分钟检测一次，data[4] = 0x0A

---

### 3.16 同步最近7天健康数据

APP发送指令，获取最近7天数据

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBC | 0x-- | 见下方 | - |

**1. 同步最近7天数据**

**data[3]** ==> 类型标识  
`0x0F`：同步最近7天所有数据

**Data[4]-data[6]** ==> 当前时间  
- Data[4] ==> 年，如2025==>25==>0x19
- Data[5] ==> 月，1-12
- Data[6] ==> 日

如2025年10月14日，Data[4]-data[6]=0x190A0E

---

#### 设备返回格式

设备根据当前时间查询最近7天所有数据，上传给APP。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBC | 0x-- | 见下方 | - |

**1. 同步最近7天数据**

**data[3]** ==> 类型标识  
`0x0F`：同步最近7天所有数据

**data[4]** ==> 数据类型  
- `0x01`：活动数据
- `0x02`：心率数据
- `0x03`：血氧数据
- `0x04`：颈椎健康数据

---

#### 1. 活动数据返回格式

**Data[5]** ==> 几条数据

**Data[6]** ==> 日

**Data[7]-data[9]** ==> 步数（单位：步）  
范围：0-60000步  
30000步 → 30000，Data[7]-data[9]=0x7530

**Data[10]-data[12]** ==> 目标消耗（单位：卡）  
3000大卡 → 3000*1000=3000000，Data[10]-data[12]=0x2DC6C0

**Data[13]-data[15]** ==> 目标距离（单位:米）  
10公里 → 10*1000=10000，Data[13]-data[15]=0x002710

**Data[6 + N * 12] ~ Data[6 + N * 12 + 11]**：第N条活动数据

> 一条活动数据包含日、步数、卡路里、距离总共12个字节

如第一条活动数据，N=0，data[6]-data[17]：
- Data[6] ==> 日
- Data[7]-data[9] ==> 步数
- Data[10]-data[12] ==> 目标消耗
- Data[13]-data[15] ==> 目标距离

| N值 | 数据范围 |
|-----|----------|
| N = 0 | 第1条数据 -> Data[6] ~ Data[17] |
| N = 1 | 第2条数据 -> Data[18] ~ Data[29] |

---

#### 2. 心率数据返回格式

**Data[5]** ==> 日

**Data[6]** ==> 时，24小时制

**Data[7]** ==> 分  
如12:00，Data[6]=0x0C，Data[7]=0x00

**Data[8]** ==> 心率值（bpm）  
如心率值80bpm，data[8]=0x50

**Data[9]** ==> 数据标识  
- `0x00` ==> 第一条数据
- `0x01` ==> 中间数据
- `0x02` ==> 结束数据（当天数据结束）
- `0x03` ==> 代表7天所有数据全部返回完成

> App端根据Data[9]数据标识02来分割每一天的数据  
> App端根据数据Data[9]标识03来判断7天所有数据已返回完成

---

#### 3. 血氧数据返回格式

**Data[5]** ==> 日

**Data[6]** ==> 时，24小时制

**Data[7]** ==> 分  
如12:00，Data[6]=0x0C，Data[7]=0x00

**Data[8]** ==> 血氧值（单位：%）  
如血氧值98%，data[8]=0x62

**Data[9]** ==> 数据标识  
- `0x00` ==> 第一条数据
- `0x01` ==> 中间数据
- `0x02` ==> 结束数据（当天数据结束）
- `0x03` ==> 代表7天所有数据全部返回完成

> App端根据数据标识02来分割每一天的数据  
> App端根据数据标识03来判断7天所有数据已返回完成

---

#### 4. 颈椎健康数据返回格式

**Data[5]** ==> 日

**Data[6]** ==> 时，24小时制

**Data[7]-data[10]** ==> 当天总佩戴时长（单位：分钟）  
如佩戴8小时，8*60=480分钟，Data[7]-data[10]=0x000001E0

**Data[11]** ==> 每小时佩戴时长（单位：分钟）  
如佩戴30分钟，data[11]=0x1E

下面5个占比为每小时的占比：

| 数据位 | 说明 |
|--------|------|
| Data[12] | Level 1占比（单位：%），如占比20%，data[12]=0x14 |
| Data[13] | Level 2占比（单位：%） |
| Data[14] | Level 3占比（单位：%） |
| Data[15] | Level 4占比（单位：%） |
| Data[16] | Level 5占比（单位：%） |

**Data[17]** ==> 数据标识  
- `0x00` ==> 第一条数据
- `0x01` ==> 中间数据
- `0x02` ==> 结束数据（当天数据结束）
- `0x03` ==> 代表7天所有颈椎健康数据全部返回完成

---

## 四、运动健康功能（0xBD）

### 4.1 运动状态设置（开启、暂停、恢复、结束）

App向设备发送运动运动状态指令或获取当前运动状态指令

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 运动状态设置**

**data[3]** ==> 类型标识  
`0x00`：运动状态设置

**Data[4]** ==> 运动状态指令  
- `0x01` ==> 开启
- `0x02` ==> 暂停
- `0x03` ==> 恢复
- `0x04` ==> 结束

**Data[5] - data[10]** ==> 状态发起时间  
- Data[5] ==> 年，如2025==>25==>0x19
- Data[6] ==> 月，1-12，12月==>0x0C
- Data[7] ==> 日
- Data[8] ==> 时，24小时制
- Data[9] ==> 分
- Data[10] ==> 秒

如2025/12/13 08时30分40秒  
Data[5]-data[10]=0x190C0D081E28

**Data[11]** ==> 运动类型  
- `0x00` ==> 室内跑步机
- `0x01` ==> 户外跑步
- `0x02` ==> 户外健走

**2. 获取最近设置运动状态**  
数据域数据：类型标识+FF ==> `0x00FF`

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动状态设置**

**data[3]** ==> 类型标识  
`0x00`：运动状态设置

**Data[4]** ==> 运动状态指令  
- `0x00` ==> 未开启（无运动状态设置记录）
- `0x01` ==> 开启
- `0x02` ==> 暂停
- `0x03` ==> 恢复
- `0x04` ==> 结束

**Data[5] - data[10]** ==> 状态发起时间  
- Data[5] ==> 年，如2025==>25==>0x19
- Data[6] ==> 月，1-12，12月==>0x0C
- Data[7] ==> 日
- Data[8] ==> 时，24小时制
- Data[9] ==> 分
- Data[10] ==> 秒

如2025/12/13 08时30分40秒  
Data[5]-data[10]=0x190C0D081E28

**Data[11]** ==> 运动类型  
- `0x00` ==> 室内跑步机
- `0x01` ==> 户外跑步
- `0x02` ==> 户外健走

**2. 获取运动状态时**  
如果当前状态为未开启，数据域返回：类型标识 + 未开启即 `0x0000` 即可

---

### 4.2 户外运动中，APP主动上报当前经纬度

运动耳机进行户外运动时，APP每秒自动上报当前定位经纬度给设备，设备无需回复。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 经纬度上报**

**data[3]** ==> 类型标识  
`0x01`：运动中经纬度上报

**经度表示：**

**Data[4]** ==> 经度正负值标识（取值范围：-180.000000 到 +180.000000，精确到六位小数）  
- `0x00` ==> 正数
- `0x01` ==> 负数

**Data[5]-data[8]** ==> 经度数据  
114.061219 ==> 114061219 ==> 0x06CC6FA3

**纬度表示：**

**Data[9]** ==> 纬度正负值标识（取值范围：-90.000000 到 +90.000000，精确到六位小数）  
- `0x00` ==> 正数
- `0x01` ==> 负数

**Data[10]-data[13]** ==> 纬度数据  
22.604802 ==> 22604802 ==> 0x0158EC02

---

### 4.3 实时运动数据上报（实时数据查看）

正在运动时，设备定时向APP上报运动实时数据，供APP UI展示（设备根据实际自行定上报时间）

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据上报**

**data[3]** ==> 类型标识  
`0x02`：设备实时上报运动数据

| 数据位 | 说明 |
|--------|------|
| Data[4]-data[6] | 运动时长（单位：秒），如3小时15分==>3\*3600+15\*60=11700，Data[4]-data[6]=0x002DB4 |
| Data[7]-data[9] | 运动距离（单位：米），10公里 → 10\*1000=10000，Data[7]-data[9]=0x002710 |
| Data[10]-data[12] | 运动步数 |
| Data[13]-data[14] | 消耗卡路里（单位：卡） |
| Data[15] | 实时心率 |
| Data[16]-data[17] | 实时配速（米/秒），如1.55 ==> 155 ==> 0x009B |
| Data[18] | 实时步频（步/分钟） |
| Data[19]-data[20] | 触地时间（毫秒） |
| Data[21]-data[22] | 腾空时间（毫秒） |
| Data[23] | 垂直振幅（厘米） |

---

### 运动总结说明

1. 运动结束，APP主动获取运动总结数据，设备传输完运动总结所有数据后，APP回复接收完成后，设备可执行删除当前数据，整理内存空间。

2. 意外结束运动，如运动中：蓝牙断开、手机APP杀掉进程，设备当记录为一次结束运动，保存运动数据总结，供设备下次连接后APP主动获取数据。

---

### 4.4 运动总结数据汇总

向设备获取运动总结数据汇总，APP发送

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x01 | 见下方 | - |

**1. 运动总结**

**data[3]** ==> 类型标识  
`0x03`：获取运动总结数据

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动总结**

**data[3]** ==> 类型标识  
`0x03`：获取运动总结数据

| 数据位 | 说明 |
|--------|------|
| Data[4]-data[9] | 运动开始时间 |
| Data[10] | 运动类型：`0x00`室内跑步机、`0x01`户外跑步、`0x02`户外健走 |
| Data[11]-data[13] | 运动时长（单位：秒），如3小时15分==>11700，0x002DB4 |
| Data[14]-data[16] | 运动距离（单位：米），10公里 → 10000，0x002710 |
| Data[17] | 最大摄氧量（mL/(kg·min)） |
| Data[18]-data[20] | 步数 |
| Data[21]-data[22] | 消耗卡路里（单位：卡） |
| Data[23]-data[24] | 最快配速（米/秒） |
| Data[25]-data[26] | 最慢配速（米/秒） |
| Data[27]-data[28] | 平均配速（米/秒），如1.55 ==> 155 ==> 0x009B |
| Data[29] | 最低心率 |
| Data[30] | 最高心率 |
| Data[31] | 平均心率 |
| Data[32] | 平均步频（步/分钟） |
| Data[33] | 有氧训练表现（0-5） |
| Data[34] | 无氧训练表现（0-5） |
| Data[35]-data[36] | 平均触地时间（毫秒） |
| Data[37]-data[38] | 平均腾空时间（毫秒） |
| Data[39] | 平均垂直振幅（厘米） |
| Data[40] | 平均左右触地平衡占比(%) |
| Data[41] | 运动恢复时长（单位：小时），运动恢复时长(算法产生，即时数据) |
| Data[42] | 运动心率区间一占比(%) |
| Data[43] | 运动心率区间二占比(%) |
| Data[44] | 运动心率区间三占比(%) |
| Data[45] | 运动心率区间四占比(%) |
| Data[46] | 运动心率区间五占比(%) |
| Data[47] | 乳酸心率区间占比(%) |
| Data[48] | 乳酸配速区间占比(%) |

---

### 4.5 运动总结分段数据

数据分段规则：每公里拆分一条数据，最后不足一公里按一条数据处理。

向设备获取运动总结分段数据，设备逐条返回分段数据，APP发送

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x01 | 见下方 | - |

**1. 运动分段**

**data[3]** ==> 类型标识  
`0x04`：获取运动总结分段数据

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动分段**

**data[3]** ==> 类型标识  
`0x04`：获取运动总结分段数据

**Data[4]** ==> 分段数据index  
- 第一条，data[4]=0x00
- 第二条，data[4]=0x01
- 第三条，...
- **最后一条返回 data[4]=0xFF**

| 数据位 | 说明 |
|--------|------|
| Data[5]-data[6] | 每公里运动时长（单位：秒） |
| Data[7]-data[8] | 配速（米/秒），如1.55 ==> 155 ==> 0x009B |
| Data[9] | 平均心率 |
| Data[10] | 平均步频（步/分钟） |

---

### 4.6 运动总结所有数据已接收完毕

通知设备，运动数据已接收完成，设备可释放内存空间。

通知设备数据完成，APP发送

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 通知设备，接收完毕指令**

**data[3]** ==> 类型标识  
`0x05`：数据接收完成

**Data[4]-data[9]** ==> 运动开始时间

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x02 | 见下方 | - |

**1. 通知设备，接收完毕指令**

**data[3]** ==> 类型标识  
`0x05`：数据接收完成

**Data[4]** = `0x00`（固定值）

---

### 4.7 获取当前设备储存运动总结条数

APP发送指令，查询当前设备储存的运动数据条数和对应的运动时间，APP会使用运动时间查询各项运动数据详情。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 运动总结查询**

**data[3]** ==> 类型标识  
`0x06`：运动数据条数查询

---

#### 设备返回格式

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动总结查询**

**data[3]** ==> 类型标识  
`0x06`：运动数据条数查询

**Data[4]** ==> 运动总结条数  
如，储存了2条数据，data[4] = 0x02

**Data[5]-data[10]** ==> 第一条运动总结的运动时间

**Data[11]-data[16]** ==> 第二条运动总结的运动时间

...如果有第三条数据，以此类推

**2. 运动总结查询--如果没有运动记录**  
返回类型标识+0xFF ==> `0x06FF` 即可。

---

### 4.8 获取一条运动数据的运动心率数据

APP发送指令，获取某一条运动数据的所有心率数据。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x07`：心率数据查询

**Data[4]-data[9]** ==> 运动开始时间

---

#### 设备返回格式

设备根据运动开始时间查询该条运动记录的所有心率数据，上传给APP。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x07`：心率数据查询

**Data[4]** ==> N+1，上报数据条数（表示这一帧数据携带几个心率数据）

**Data[5 + N * 5] ~ Data[5 + N * 5 + 4]**：第N条心率数据

> 一条心率数据包含时间（日时分秒）+心率数据，总共5个字节

如第一条心率数据，N=0，data[5]-data[9]：
- Data[5]-data[8] ==> 日时分秒
- Data[9] ==> 心率数据

| N值 | 数据范围 |
|-----|----------|
| N = 0 | 第1条数据 -> Data[5] ~ Data[9] |
| N = 1 | 第2条数据 -> Data[10] ~ Data[14] |
| N = 2 | 第3条数据 -> Data[15] ~ Data[19] |

> 特别的，一帧数据大小建议不要超过180，所以建议N+1<35条。

---

### 4.9 获取一条运动数据的运动配速数据

APP发送指令，获取某一条运动数据的所有配速数据。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x08`：运动配速数据查询

**Data[4]-data[9]** ==> 运动开始时间

---

#### 设备返回格式

设备根据运动开始时间查询该条运动记录的所有配速数据，上传给APP。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x08`：配速数据查询

**Data[4]** ==> N+1，上报数据条数（表示这一帧数据携带几个运动配速数据）

**Data[5 + N * 6] ~ Data[5 + N * 6 + 5]**：第N条数据

> 一条数据包含时间（日时分秒）+配速数据，总共6个字节

如第一条数据，N=0，data[5]-data[10]：
- Data[5]-data[8] ==> 日时分秒
- Data[9]-data[10] ==> 配速数据（米/秒），如1.55 ==> 155 ==> 0x009B

| N值 | 数据范围 |
|-----|----------|
| N = 0 | 第1条数据 -> Data[5] ~ Data[10] |
| N = 1 | 第2条数据 -> Data[11] ~ Data[16] |
| N = 2 | 第3条数据 -> Data[17] ~ Data[22] |

> 特别的，一帧数据大小建议不要超过180，所以建议N+1<35条。

---

### 4.10 获取一条运动数据的运动步频数据

APP发送指令，获取某一条运动数据的所有步频数据。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x09`：运动步频数据查询

**Data[4]-data[9]** ==> 运动开始时间

---

#### 设备返回格式

设备根据运动开始时间查询该条运动记录的所有步频数据，上传给APP。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x09`：步频数据查询

**Data[4]** ==> N+1，上报数据条数（表示这一帧数据携带几个运动步频数据）

**Data[5 + N * 5] ~ Data[5 + N * 5 + 4]**：第N条数据

> 一条数据包含时间（日时分秒）+步频数据，总共5个字节

如第一条数据，N=0，data[5]-data[9]：
- Data[5]-data[8] ==> 日时分秒
- Data[9] ==> 步频数据（步/分钟）

| N值 | 数据范围 |
|-----|----------|
| N = 0 | 第1条数据 -> Data[5] ~ Data[9] |
| N = 1 | 第2条数据 -> Data[10] ~ Data[14] |
| N = 2 | 第3条数据 -> Data[15] ~ Data[19] |

> 特别的，一帧数据大小建议不要超过180，所以建议N+1<35条。

---

### 4.11 获取一条运动数据的运动轨迹数据（GPS位置数据）

APP发送指令，获取某一条运动数据的所有定位数据。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xAA | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x0A`：运动定位数据查询

**Data[4]-data[9]** ==> 运动开始时间

---

#### 设备返回格式

设备根据运动开始时间查询该条运动记录的所有定位数据，上传给APP。

| 协议头 | 字段类型 | 长度 | 数据域 | 校验和 |
|--------|----------|------|--------|--------|
| 0xA5 | 0xBD | 0x-- | 见下方 | - |

**1. 运动数据详情获取**

**data[3]** ==> 类型标识  
`0x0A`：定位数据查询

**Data[4]** ==> N+1，上报数据条数（表示这一帧数据携带几个运动GPS定位数据）

**Data[5 + N * 14] ~ Data[5 + N * 14 + 13]**：第N条数据

> 一条数据包含时间（日时分秒）+定位数据，总共14个字节

如第一条数据，N=0，data[5]-data[18]：

**Data[5]-data[8]** ==> 日时分秒

**Data[9]-data[18]** ==> 经纬度

**经度表示：**
- Data[9] ==> 经度正负值标识（取值范围：-180.000000 到 +180.000000，精确到两位小数）
  - `0x00` ==> 正数
  - `0x01` ==> 负数
- Data[10]-data[13] ==> 经度数据，如114.061219==>114061219==>0x06CC6FA3

**纬度表示：**
- Data[14] ==> 纬度正负值标识（取值范围：-90.000000 到 +90.000000，精确到两位小数）
  - `0x00` ==> 正数
  - `0x01` ==> 负数
- Data[15]-data[18] ==> 纬度数据，如22.604802==>22604802==>0x0158EC02

| N值 | 数据范围 |
|-----|----------|
| N = 0 | 第1条数据 -> Data[5] ~ Data[18] |
| N = 1 | 第2条数据 -> Data[19] ~ Data[32] |
| N = 2 | 第3条数据 -> Data[33] ~ Data[46] |

> 特别的，一帧数据大小建议不要超过180，所以建议N+1<17条。

---

## 协议常量速查表

### 字段类型 (FIELD_TYPE)

| 值 | 说明 |
|----|------|
| 0xBA | 设备信息同步 |
| 0xBB | 运动目标 |
| 0xBC | 健康检测 |
| 0xBD | 运动健康 |

### 协议头

| 值 | 说明 |
|----|------|
| 0xAA | APP发送指令 |
| 0xA5 | 设备返回数据 |

### 数据标识

| 值 | 说明 |
|----|------|
| 0x00 | 第一条数据 |
| 0x01 | 中间数据 |
| 0x02 | 结束数据（当天/分段数据结束） |
| 0x03 | 7天数据全部返回完成 |
| 0xFF | 主动上报/获取默认值/最后一条分段数据 |

### 运动状态

| 值 | 说明 |
|----|------|
| 0x00 | 未开启 |
| 0x01 | 开启 |
| 0x02 | 暂停 |
| 0x03 | 恢复 |
| 0x04 | 结束 |

### 运动类型

| 值 | 说明 |
|----|------|
| 0x00 | 室内跑步机 |
| 0x01 | 户外跑步 |
| 0x02 | 户外健走 |
